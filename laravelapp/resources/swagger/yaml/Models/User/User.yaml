components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        email:
          type: string
        player:
          $ref: "#/components/schemas/Player"
