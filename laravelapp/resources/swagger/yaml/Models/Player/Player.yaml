components:
  schemas:
    Player:
      type: object
      required:
        - nickname
      properties:
        id:
          type: integer
        user_id:
          type: integer
          description: 'player owner id'
        nickname:
          type: string
        session:
          $ref: "#/components/schemas/Session"
